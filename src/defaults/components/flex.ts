import { BuitUIProps } from '../../types';
import { ComponentCtx } from '../theme';

const base: BuitUIProps = {
  display: 'flex',
};

const variants = {};

export const flex: ComponentCtx = {
  base,
  variants,
  defaultVariant: '',
};

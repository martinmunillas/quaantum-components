import { QuaantumUIProps } from '../../../types';
import { ComponentCtx } from '../../../defaults/theme';

const base: QuaantumUIProps = {
  display: 'flex',
};

const variants = {};

export const Flex: ComponentCtx = {
  base,
  variants,
  defaultVariant: '',
};

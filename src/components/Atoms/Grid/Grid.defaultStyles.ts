import { QuaantumProps } from '../../../css/types';
import { ComponentCtx } from '../../../defaults/theme';

const base: QuaantumProps = {
  display: 'grid',
};

const variants = {};

export const Grid: ComponentCtx = {
  base,
  variants,
  defaultVariant: '',
};

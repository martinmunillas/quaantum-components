import { BuitUIProps } from '../../../types';
import { ComponentCtx } from '../../../defaults/theme';

const base: BuitUIProps = {};

const variants = {};

export const Box: ComponentCtx = {
  base,
  variants,
  defaultVariant: '',
};
